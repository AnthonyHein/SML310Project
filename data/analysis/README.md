Contains data files and labels for the pairwise winner input, split into `train`, `dev`, and `test` files.

Also contains binaries and metadata about model training in individual folders by model class.